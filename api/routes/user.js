const router = require('express').Router();

/**
 * Register user
 * 
 * walletAddress
 * firstName
 * lastName
 * extension
 * about
 */
router.post('/');

/**
 * Get user
 * 
 * walletAddress - optional [other | self]
 */
router.get('/')

/**
 * Update user
 */
router.patch('/');

module.exports = router;